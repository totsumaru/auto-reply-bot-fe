import React from "react";
import {Box, Button} from "@mui/material";

export const AddKeywordBtn = () => {
  return (
    <Box sx={{m: 1}}>
      <Button variant="contained">キーワードを追加</Button>
    </Box>
  )
}